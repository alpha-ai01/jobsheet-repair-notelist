# Security Policy

## Project

**SmartRepair - ระบบจัดการงานซ่อม**

ระบบ SmartRepair เป็นระบบจัดการงานซ่อมภายในสำหรับร้านหรือกลุ่มผู้ใช้งาน
โดยให้ความสำคัญกับการควบคุมสิทธิ์ การปกป้องข้อมูลลูกค้า และการแยกข้อมูลระหว่างกลุ่ม

## Security Scope

ระบบใช้เทคโนโลยีหลัก ได้แก่:

- Firebase Authentication สำหรับการยืนยันตัวตน
- Cloud Firestore สำหรับจัดเก็บข้อมูล
- Firestore Security Rules สำหรับควบคุมสิทธิ์การเข้าถึงข้อมูล
- GitHub Pages สำหรับการเผยแพร่หน้าเว็บ

## Access Control

ระบบรองรับการแบ่งระดับสิทธิ์ตามบทบาทของผู้ใช้งาน

### Admin

ผู้ดูแลระบบระดับสูงสุด

- จัดการผู้ใช้งาน
- จัดการสิทธิ์ของผู้ใช้งาน
- จัดการข้อมูลระบบ
- เข้าถึงฟังก์ชันสำหรับผู้ดูแลระบบตามที่กำหนดใน Firestore Security Rules

### Owner

เจ้าของกลุ่ม

- จัดการสมาชิกภายในกลุ่ม
- เพิ่มหรือเชิญสมาชิก
- ปิดการใช้งานสมาชิก
- เปลี่ยนบทบาทของสมาชิกภายในกลุ่ม
- แก้ไขข้อมูลของกลุ่ม
- ดูข้อมูลและงานของกลุ่ม

Owner ไม่สามารถยกระดับสิทธิ์ของตนเองเป็น Admin ผ่านระบบหรือ Firestore Client ได้

### Manager / Member

สมาชิกทั่วไปของกลุ่ม

- ใช้งานระบบตามสิทธิ์ที่ได้รับ
- สร้างและจัดการงานซ่อมตามสิทธิ์
- ดูข้อมูลของกลุ่มตามสิทธิ์
- ใช้งานข้อมูลลูกค้าตามสิทธิ์ที่ระบบกำหนด

Manager / Member ไม่สามารถ:

- เพิ่มหรือลบสมาชิก
- เปลี่ยน Owner
- เปลี่ยนสิทธิ์ของสมาชิก
- แก้ไขข้อมูลกลุ่ม
- ยกระดับสิทธิ์ของตนเอง

## Group Data Isolation

ข้อมูลที่เป็นของกลุ่มควรถูกผูกกับ `groupId`

สมาชิกของกลุ่มสามารถเข้าถึงข้อมูลของกลุ่มตนเองได้ตามสิทธิ์เท่านั้น

ข้อมูลสำคัญที่เกี่ยวข้องกับกลุ่มอาจรวมถึง:

- Repairs
- Customers
- Group Members
- Group Settings

Firestore Security Rules เป็นกลไกหลักในการบังคับใช้การแยกข้อมูลระหว่างกลุ่ม

## Authentication

ระบบใช้ Firebase Authentication ในการยืนยันตัวตน

การกำหนดสิทธิ์ไม่ควรพึ่งพาการซ่อนหรือแสดงเมนูในหน้าเว็บเพียงอย่างเดียว

การอนุญาตให้เข้าถึงข้อมูลต้องถูกตรวจสอบโดย Firestore Security Rules ด้วย

## Firestore Security

Firestore Security Rules ควรปฏิบัติตามหลักการดังต่อไปนี้:

- ผู้ใช้ที่ไม่ได้เข้าสู่ระบบต้องไม่สามารถเข้าถึงข้อมูลภายในระบบ
- ผู้ใช้ที่ถูกปิดใช้งานต้องไม่สามารถใช้งานข้อมูลที่ได้รับการป้องกัน
- ผู้ใช้ทั่วไปไม่สามารถเปลี่ยนบทบาทของตนเองเป็น Admin หรือ Owner
- Manager / Member ไม่สามารถแก้ไขข้อมูลสมาชิกหรือข้อมูลกลุ่ม
- ผู้ใช้สามารถเข้าถึงข้อมูลของกลุ่มได้เฉพาะเมื่อมีความสัมพันธ์กับกลุ่มนั้น
- การเขียนข้อมูลต้องตรวจสอบ `createdBy` และ `groupId` ตามประเภทข้อมูล
- สิทธิ์ระดับ Admin ต้องถูกตรวจสอบจากข้อมูลที่เชื่อถือได้ใน Firestore

## Data Protection

ข้อมูลที่ควรได้รับการปกป้องเป็นพิเศษ ได้แก่:

- ข้อมูลบัญชีผู้ใช้งาน
- อีเมล
- เบอร์โทรศัพท์ลูกค้า
- ข้อมูลลูกค้า
- ข้อมูลอุปกรณ์
- รายละเอียดงานซ่อม
- ประวัติการดำเนินงาน
- ข้อมูลสมาชิกและสิทธิ์
- ข้อมูลภายในกลุ่ม

ไม่ควรใส่ข้อมูลลับ เช่น API Keys, Passwords หรือ Firebase Admin Credentials ลงใน Repository

## Client-Side Security

การซ่อนปุ่มหรือเมนูใน `index.html` ไม่ถือเป็นมาตรการรักษาความปลอดภัยเพียงพอ

ตัว Client อาจถูกตรวจสอบหรือแก้ไขโดยผู้ใช้งานได้

ดังนั้นการป้องกันข้อมูลและการอนุญาตให้เขียนข้อมูลต้องบังคับใช้ที่ Firestore Security Rules เป็นหลัก

## Reporting a Vulnerability

หากพบช่องโหว่ด้านความปลอดภัยของ SmartRepair กรุณารายงานให้ผู้ดูแล Repository ทราบโดยตรง

โปรดหลีกเลี่ยงการเผยแพร่รายละเอียดของช่องโหว่ต่อสาธารณะก่อนที่จะมีโอกาสตรวจสอบและแก้ไขปัญหา

รายงานควรระบุ:

- รายละเอียดของช่องโหว่
- ขั้นตอนที่ทำให้เกิดปัญหา
- ผลกระทบที่อาจเกิดขึ้น
- วิธีการทำซ้ำปัญหา หากสามารถทำได้
- ข้อเสนอแนะในการแก้ไข หากมี

## Security Changes

การเปลี่ยนแปลงด้านความปลอดภัยควร:

1. ตรวจสอบผลกระทบต่อระบบเดิม
2. ทดสอบ Firestore Security Rules
3. ตรวจสอบสิทธิ์ของแต่ละ Role
4. ตรวจสอบการแยกข้อมูลระหว่างกลุ่ม
5. ตรวจสอบว่าไม่ทำให้ฟังก์ชันเดิมของ SmartRepair เสียหาย
6. Commit การเปลี่ยนแปลงอย่างชัดเจนเพื่อให้สามารถตรวจสอบและย้อนกลับได้

---

**SmartRepair Security Policy**

เอกสารนี้เป็นแนวทางด้านความปลอดภัยของโปรเจกต์ และไม่ใช่ตัวกำหนด Firestore Security Rules โดยตรง
